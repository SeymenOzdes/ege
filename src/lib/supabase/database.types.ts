/**
 * Generated with `pnpm supabase:types` after the local schema exists.
 *
 * Module 3 intentionally has no application tables yet. This empty baseline
 * keeps the browser and server clients typed until Module 4 adds the schema.
 */
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
    CompositeTypes: Record<string, never>;
  };
};
